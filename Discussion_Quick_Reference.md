# ورقة المراجعة السريعة للمناقشة
# Discussion Quick Reference Sheet

## نظام سارة لإدارة الحضور
**التاريخ:** ديسمبر 2024

---

## 1. البرامج المستخدمة

| البرنامج | الغرض |
|----------|-------|
| **Replit** | بيئة التطوير والاستضافة |
| **VS Code** | محرر الأكواد (اختياري) |
| **PostgreSQL** | قاعدة البيانات |
| **Git / GitHub** | إدارة الإصدارات |
| **Chrome DevTools** | اختبار وتصحيح الأخطاء |
| **Postman** | اختبار API (اختياري) |

---

## 2. لغات البرمجة المستخدمة

| اللغة | الاستخدام | المكان |
|-------|-----------|--------|
| **TypeScript** | الواجهة الأمامية + الخلفية | كل الملفات `.ts` و `.tsx` |
| **JavaScript** | أساس TypeScript | يُترجم من TypeScript |
| **SQL** | استعلامات قاعدة البيانات | Drizzle ORM |
| **HTML** | هيكل الصفحات | داخل ملفات React (JSX) |
| **CSS** | التصميم | Tailwind CSS |

---

## 3. التقنيات (Technologies)

### Frontend (الواجهة الأمامية)
| التقنية | النوع | الغرض |
|---------|-------|-------|
| **React 18** | Framework | بناء واجهة المستخدم التفاعلية |
| **TypeScript** | Language | إضافة Type Safety للكود |
| **Tailwind CSS** | CSS Framework | تصميم سريع ومتجاوب |
| **shadcn/ui** | UI Components | مكونات جاهزة (أزرار، نماذج، جداول) |
| **TanStack Query** | Data Fetching | جلب البيانات من الخادم |
| **Wouter** | Router | التنقل بين الصفحات |
| **FingerprintJS** | Library | بصمة الجهاز الفريدة |
| **Pigeon Maps** | Library | عرض الخرائط |

### Backend (الخادم)
| التقنية | النوع | الغرض |
|---------|-------|-------|
| **Node.js** | Runtime | تشغيل JavaScript على الخادم |
| **Express.js** | Framework | بناء REST API |
| **TypeScript** | Language | Type Safety |
| **Drizzle ORM** | ORM | التعامل مع قاعدة البيانات |
| **bcryptjs** | Library | تشفير كلمات المرور |
| **express-session** | Library | إدارة الجلسات |
| **Zod** | Library | التحقق من صحة البيانات |

### Database (قاعدة البيانات)
| التقنية | النوع | الغرض |
|---------|-------|-------|
| **PostgreSQL** | RDBMS | تخزين البيانات |
| **Neon** | Cloud DB | استضافة قاعدة البيانات |

### Security (الأمان)
| التقنية | الغرض |
|---------|-------|
| **AES-256-CBC** | تشفير بصمة الجهاز |
| **bcrypt** | تشفير كلمات المرور |
| **HTTPS/TLS** | تشفير الاتصال |
| **Haversine Formula** | حساب المسافة GPS |

---

## 4. أماكن الأكواد - كل كود وش يشغل

### الملفات الرئيسية

| الملف | وش يشغل |
|-------|---------|
| `client/src/App.tsx` | **التطبيق الرئيسي** - تعريف الصفحات والتنقل |
| `client/src/main.tsx` | **نقطة الدخول** - تشغيل React |
| `server/index.ts` | **تشغيل الخادم** - Express server |
| `server/routes.ts` | **نقاط API** - كل الـ endpoints |
| `server/storage.ts` | **طبقة التخزين** - CRUD operations |
| `shared/schema.ts` | **جداول قاعدة البيانات** - تعريف الجداول |

### صفحات الواجهة (client/src/pages/)

| الملف | وش يشغل |
|-------|---------|
| `landing.tsx` | صفحة الترحيب الرئيسية |
| `login.tsx` | صفحة تسجيل الدخول |
| `dashboard.tsx` | لوحة التحكم الرئيسية |
| `attendance/employee-dashboard.tsx` | **لوحة المتدرب** - تسجيل الحضور والانصراف |
| `attendance/supervisor-dashboard.tsx` | لوحة المشرف |
| `hrm/employee-list.tsx` | قائمة الموظفين |
| `hrm/add-employee.tsx` | إضافة موظف جديد |
| `departments/department-list.tsx` | قائمة الأقسام |
| `settings/allowed-locations.tsx` | **إدارة المواقع GPS** |
| `settings/work-schedules.tsx` | جداول العمل |
| `reports.tsx` | التقارير |
| `device-requests.tsx` | طلبات تغيير الأجهزة |

### ملفات الأمان (server/)

| الملف | وش يشغل |
|-------|---------|
| `utils/security.ts` | **التشفير** - AES-256, bcrypt, Haversine |
| `middleware/attendance-security.ts` | **التحقق من الحضور** - جهاز + موقع |

---

## 5. قاعدة البيانات

### معلومات عامة
- **النوع:** PostgreSQL (علائقية - Relational)
- **الاستضافة:** Neon Cloud
- **ORM:** Drizzle ORM
- **ملف التعريف:** `shared/schema.ts`

### الجداول الرئيسية

| الجدول | الوظيفة | الحقول الرئيسية |
|--------|---------|-----------------|
| `users` | المستخدمين | id, username, password, name, role, deviceFingerprint |
| `attendance_records` | سجلات الحضور | id, userId, type, timestamp, latitude, longitude |
| `allowed_locations` | المواقع المصرح بها | id, name, latitude, longitude, radiusMeters |
| `locations` | الأقسام | id, name, description |
| `device_change_requests` | طلبات تغيير الأجهزة | id, userId, status, reason |
| `work_schedules` | جداول العمل | id, name, startTime, endTime, daysOfWeek |
| `attendance_attempts` | محاولات التسجيل | id, userId, status, failureReason |

### العلاقات
```
users ──────────► attendance_records (1:N)
users ──────────► device_change_requests (1:N)
locations ──────► users (1:N) [القسم]
locations ──────► allowed_locations (1:N)
```

---

## 6 & 7. أسئلة "هل استخدمتي...؟"

### ✅ نعم استخدمت

| التقنية/اللغة | نعم/لا | أين استخدمتها |
|---------------|--------|---------------|
| React | ✅ نعم | كل الواجهة الأمامية |
| TypeScript | ✅ نعم | Frontend + Backend |
| Node.js | ✅ نعم | الخادم |
| Express.js | ✅ نعم | REST API |
| PostgreSQL | ✅ نعم | قاعدة البيانات |
| Tailwind CSS | ✅ نعم | التصميم |
| REST API | ✅ نعم | التواصل بين Frontend و Backend |
| تشفير AES-256 | ✅ نعم | بصمة الجهاز |
| bcrypt | ✅ نعم | كلمات المرور |
| GPS/Geolocation | ✅ نعم | التحقق من الموقع |
| ORM | ✅ نعم | Drizzle ORM |
| Session-based Auth | ✅ نعم | تسجيل الدخول |
| Responsive Design | ✅ نعم | يعمل على الجوال والكمبيوتر |
| RTL Support | ✅ نعم | دعم اللغة العربية |

### ❌ لم أستخدم

| التقنية/اللغة | نعم/لا | لماذا لم أستخدمها |
|---------------|--------|-------------------|
| Python | ❌ لا | استخدمت TypeScript/Node.js |
| Java | ❌ لا | استخدمت TypeScript/Node.js |
| PHP | ❌ لا | استخدمت TypeScript/Node.js |
| MySQL | ❌ لا | استخدمت PostgreSQL |
| MongoDB | ❌ لا | استخدمت PostgreSQL (علائقية) |
| Angular | ❌ لا | استخدمت React |
| Vue.js | ❌ لا | استخدمت React |
| JWT | ❌ لا | استخدمت Session-based Auth |
| Firebase | ❌ لا | استخدمت PostgreSQL + Express |
| AWS | ❌ لا | استخدمت Replit للاستضافة |

---

## ملخص سريع للحفظ

```
🔷 اللغة الرئيسية: TypeScript
🔷 Frontend: React + Tailwind CSS
🔷 Backend: Node.js + Express.js
🔷 قاعدة البيانات: PostgreSQL
🔷 ORM: Drizzle
🔷 الأمان: AES-256 + bcrypt + GPS
🔷 الاستضافة: Replit
🔷 نظام التشغيل: يعمل على الكل (Web App)
```

---

## نصائح للمناقشة

1. **إذا سألوك عن تقنية ما استخدمتيها:**
   - قولي "استخدمت [البديل] لأنه مناسب أكثر للمشروع"
   - مثال: "ما استخدمت MongoDB لأن البيانات عندي علائقية وتحتاج PostgreSQL"

2. **إذا سألوك عن كود معين:**
   - اشرحي الهدف أولاً ثم الطريقة
   - مثال: "هذا الكود يتحقق من موقع المتدرب قبل تسجيل الحضور"

3. **إذا سألوك عن الأمان:**
   - اذكري: تشفير AES-256، bcrypt، التحقق من الجهاز والموقع

4. **إذا سألوك ليش هذه التقنية:**
   - React: أشهر مكتبة، مجتمع كبير، سهولة التطوير
   - TypeScript: يقلل الأخطاء، Type Safety
   - PostgreSQL: موثوقة، قوية، مجانية

---

**بالتوفيق في المناقشة! 🎓**
